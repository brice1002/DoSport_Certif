{% extends 'base.html.twig' %}

{% block title %}Modifier mon mot de passe - Do Sport{% endblock %}

{% block content %}
    <h1>Modifier mon mot de passe</h1>
    <a href="{{ path('account') }}">Retourner à votre compte</a> <br/>
 
    <hr>
    {% if notification %}
        <div class="alert alert-info">
            {{ notification }}
        </div>
    {% endif %}
    
    {{ form(form) }}

    - Ajouter mes adresses <br/>
    - Mes commandes <br/>

{# <pre>
    {{ dump(app.user.firstname) }}
</pre> #}
{% endblock %}